doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Meetus
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
        link(rel='stylesheet', href="/public/home.css")
    body
        div#wait
            header
                h1 Meetus
            main     
        div#loginRoom
            form
                p 
                    | Start video call & messaging
                    br
                    | by entering a chat room
                input(id="nickname", placeholder="nickname", required, type="text")
                input(id="password", placeholder="password", required, type="text")
                button Enter Room
        div#waitRoom-container.my-element
            div#waitRoom
                div#settings
                    button#home ⌂
                    button#person ☺
                    button#clock 📃
                    button#setting ⚙       
                div#userScreen
                    video#myVideo(autoplay, playinline, width="1665", height="970")
                    button#video 🖵
                    button#audio 🔇
                    button#join-button 참여
                div#roomModal.modal
                    .modal-content  
                        p 
                            span(style="font-size: 100px; font-weight: bold") ✉ 
                            br
                            span(style="font-size: 25px; font-weight: bold; display: block; margin-bottom: 10px;") 회의 참여
                        input(id="roomName", placeholder="회의 ID 혹은 초대 링크", required, type="text")
                        button.close(type="button") 취소
                        button#joinRoomButton 확인
                        

        div#callRoom
            div#header
            div#screenStream 
                //- button#audio Mute
                //- select#cameraSelect 
            div#peerStream
                //-video#peerScreen(autoplay, playinline, width="300", height="300")
                video#peerVideo(autoplay, playinline, width = "640", height = "640")
            div#footer
            
                button#video1 🖵
            div#chatContainer
            
                ul#messages 
                form#chat
                    button Send 
                    input(type="text")


                //- div#videoCall
                //-     button#video 카메라
                //-     button#screen 화면 공유
                //-     div#screenStream
                //-         video#myVideo(autoplay, playinline, width="650", height="650")
                //-         button#audio Mute
                //-         select#cameraSelect 
                //-     div#peerStream
                //-         //video#peerScreen(autoplay, playinline, width="300", height="300")
                //-         video#peerVideo(autoplay, playinline, width = "640", height = "640")
                
        script(src="/socket.io/socket.io.js") 
        script(src="/public/js/app.js") 
        script(src = "https://docs.opencv.org/master/opencv.js")